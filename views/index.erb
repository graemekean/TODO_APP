<section>
  <% @items.each do |item| %>
  <article>
    <dl>
      <dt>Item:</dt>
      <dd><%= item['item'] %></dd>
    </dl>
    <a href="/items/<%= item['id'] %>">Show</a>
  </article>
  <% end %>
</section>